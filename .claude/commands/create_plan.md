---
description: Create detailed implementation plans through the shared Go planner CLI
---

Research the task, produce the expected plan JSON, run `~/.claude/bin/planner create-plan`, and write the rendered issue to the vault.

Use the shared create-plan engine contract and validation rules.

Do not emit freeform markdown directly when the installed helper is available.
