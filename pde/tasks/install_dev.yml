- name: Install necessary packages
  apt:
    name:
      - fzf
      - ripgrep
      - bat
      - trash-cli
    state: present

- name: Install lazyvim
  become: yes
  become_user: "{{ user }}"
  git:
    repo: https://github.com/LazyVim/starter
    dest: "{{ app_data_dir }}/nvim"
    force: yes

- name: Remove starter git data
  become: yes
  become_user: "{{ user }}"
  shell: >
    rm -rf ~/.config/nvim/.git
