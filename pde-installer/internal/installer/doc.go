// Package installer defines the pde-installer command and workflow.
// Install and update reconcile every component. Config only migrates legacy
// state and applies the repository's chezmoi source.
package installer
